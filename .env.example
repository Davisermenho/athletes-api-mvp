# .env.example
# DON'T COMMIT SECRETS: this file contains placeholders only.
# Copy to `.env` locally and fill real secrets in your environment.

# Application
APP_ENV=development
FASTAPI_HOST=127.0.0.1
FASTAPI_PORT=8000
LOG_LEVEL=info

# Database (Postgres)
# Example URL format for SQLAlchemy / asyncpg:
# postgresql://<user>:<password>@<host>:<port>/<db_name>
DATABASE_URL=postgresql://user:password@localhost:5432/athletes_db
DB_HOST=localhost
DB_PORT=5432
DB_USER=user
DB_PASSWORD=
DB_NAME=athletes_db

# Alembic (optional override; if set, Alembic uses this URL)
ALEMBIC_DATABASE_URL=postgresql://user:password@localhost:5432/athletes_db

# Security
# Generate a long random SECRET_KEY for production (do NOT commit it)
SECRET_KEY=replace-me-with-a-secure-random-value
SENTRY_DSN=

# Optional services
REDIS_URL=redis://localhost:6379/0

# Notes:
# - Keep real secrets out of the repo. Use your CI/CD secret store or
#   environment-specific secret manager (e.g. GitHub Actions Secrets,
#   AWS Secrets Manager, GCP Secret Manager, etc.).
# - After copying to `.env`, export or load variables before running the app.
# Example environment variables (do NOT put secrets here)
DATABASE_URL=postgresql://user:password@localhost:5432/athletes_db
SECRET_KEY=changeme
SENTRY_DSN=
